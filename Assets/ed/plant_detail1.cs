using UnityEngine;

public class plant_detail1 : MonoBehaviour
{

    public GameObject stem;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
